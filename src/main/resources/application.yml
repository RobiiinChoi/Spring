server:
  port: 8080

spring:
#  thymeleaf:
#    cache: false
#    prefix: classpath:/templates/
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # DB Source
    url: jdbc:mysql://database-homeworkweek1.cbyqhfpxre3k.ap-northeast-2.rds.amazonaws.com:3306/myhomework?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    # DB username
    username: adminadmin123123
    # DB password
    password: Test1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug